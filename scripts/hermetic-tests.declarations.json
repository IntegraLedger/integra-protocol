{
  "$comment": "check:hermetic-tests' exception table for integra-protocol, held in the TREE rather than inside the gate: a table baked into a script makes the gate undrivable, because it can only ever run against the one tree whose hosts it already lists. Each namedNotCalled key is a non-loopback host a NON-LIVE test names — or that a module such a test imports names — with the reason it is a value rather than a call. Adding an entry is a claim that the host is never fetched; read the occurrence before making it. CLOSED IN BOTH DIRECTIONS: an unlisted host fails, and a listed host that appears nowhere fails. NOTE: a live rail harness here is integration*.test.ts, not commerce's *.live.test.ts — see live-harness-files.mjs.",
  "namedNotCalled": {
    "93.184.216.34": "A STUBBED DNS LOOKUP RESULT, not a destination. `resolver.test.ts` injects both the lookup and `fetchImpl`, and this address is the stand-in for 'some public address' against which the hardened resolver's refusals are proven — beside 169.254.169.254, the cloud metadata address the resolver must refuse. Nothing is dialled; the point of the suite is which addresses are REJECTED.",
    "[2606:4700:4700::1111]": "The 'by address' half of the same plant, and the one that proves the fix. See `one.one.one.one` above for the pairing and for why neither is ever dialled. ⭐ RESERVED already covers `[::1]`, so loopback as an IPv6 literal stays hermetic without an entry — which a sibling case in the same drive asserts, so this exemption cannot quietly widen to cover it.",
    "api.blockcypher.com": "⭐ THE PLANTED DEFECT INSIDE THIS GATE'S OWN DRIVE, and nothing else in the tree. `rail-invariants/test/hermetic-tests-invariant.test.ts` writes this host into TEMPORARY fixture trees and asserts the gate refuses it — in a test file, and in a helper a test imports. It is never fetched: the drive's fixtures are `mkdtemp` directories the gate reads and nothing executes. ⚠️ It needs an entry here only because this repository's drives live under `packages/`, which the gate walks; the sibling repository's drives sit in `scripts/`, outside its own subject set. ⛔ Closed in both directions: if the drive stops planting this host, this entry goes stale and the gate refuses it — so the exemption cannot outlive the plant.",
    "api.devnet.solana.com": "An endpoint this rail's manifest ADVERTISES, asserted as a value by the manifest's own constants test. That file contains no call site — swept for `fetch`, `axios`, `.request(` and every rail SDK's client constructor, 0 hits across all 23 files that name a host.",
    "api.mainnet-beta.solana.com": "An endpoint this rail's manifest ADVERTISES, asserted as a value by the manifest's own constants test. That file contains no call site — swept for `fetch`, `axios`, `.request(` and every rail SDK's client constructor, 0 hits across all 23 files that name a host.",
    "cardano-mainnet.blockfrost.io": "An endpoint this rail's manifest ADVERTISES, asserted as a value by the manifest's own constants test. That file contains no call site — swept for `fetch`, `axios`, `.request(` and every rail SDK's client constructor, 0 hits across all 23 files that name a host.",
    "cardano-preprod.blockfrost.io": "An endpoint this rail's manifest ADVERTISES, asserted as a value by the manifest's own constants test. That file contains no call site — swept for `fetch`, `axios`, `.request(` and every rail SDK's client constructor, 0 hits across all 23 files that name a host.",
    "cardanoscan.io": "An endpoint this rail's manifest ADVERTISES, asserted as a value by the manifest's own constants test. That file contains no call site — swept for `fetch`, `axios`, `.request(` and every rail SDK's client constructor, 0 hits across all 23 files that name a host.",
    "claude.ai": "⭐ A SAMPLE INSIDE THE DISCLOSURE GATE'S OWN LIST, and nothing else in the tree. `scripts/forbidden-literals.mjs` carries a session-URL sample that its session-URL pattern must match, so the gate can prove that pattern still discriminates before trusting it — a pattern that has stopped matching reports every file clean, which is indistinguishable from success. It is a string handed to `RegExp.test`, never a destination: `check-forbidden-literals.mjs` opens no socket and reads the tree with `readFileSync` alone. ⛔ Deleting the sample to satisfy this gate would leave the pattern unproven, which is the exact defect the sample exists to prevent.",
    "docs.integraledger.io": "A documentation URL appearing inside capability fixtures as advertised data. `capability.test.ts` asserts what a document SAYS, and contains no call site.",
    "explore.tempo.xyz": "An endpoint this rail's manifest ADVERTISES, asserted as a value by the manifest's own constants test. That file contains no call site — swept for `fetch`, `axios`, `.request(` and every rail SDK's client constructor, 0 hits across all 23 files that name a host.",
    "explore.testnet.tempo.xyz": "An endpoint this rail's manifest ADVERTISES, asserted as a value by the manifest's own constants test. That file contains no call site — swept for `fetch`, `axios`, `.request(` and every rail SDK's client constructor, 0 hits across all 23 files that name a host.",
    "explorer.aptoslabs.com": "An endpoint this rail's manifest ADVERTISES, asserted as a value by the manifest's own constants test. That file contains no call site — swept for `fetch`, `axios`, `.request(` and every rail SDK's client constructor, 0 hits across all 23 files that name a host.",
    "faucet.altnet.rippletest.net": "An endpoint this rail's manifest ADVERTISES, asserted as a value by the manifest's own constants test. That file contains no call site — swept for `fetch`, `axios`, `.request(` and every rail SDK's client constructor, 0 hits across all 23 files that name a host.",
    "fullnode.mainnet.sui.io": "An endpoint this rail's manifest ADVERTISES, asserted as a value by the manifest's own constants test. That file contains no call site — swept for `fetch`, `axios`, `.request(` and every rail SDK's client constructor, 0 hits across all 23 files that name a host.",
    "fullnode.testnet.aptoslabs.com": "An endpoint this rail's manifest ADVERTISES, asserted as a value by the manifest's own constants test. That file contains no call site — swept for `fetch`, `axios`, `.request(` and every rail SDK's client constructor, 0 hits across all 23 files that name a host.",
    "fullnode.testnet.sui.io": "An endpoint this rail's manifest ADVERTISES, asserted as a value by the manifest's own constants test. That file contains no call site — swept for `fetch`, `axios`, `.request(` and every rail SDK's client constructor, 0 hits across all 23 files that name a host.",
    "graphql.mainnet.sui.io": "An endpoint this rail's manifest ADVERTISES, asserted as a value by the manifest's own constants test. That file contains no call site — swept for `fetch`, `axios`, `.request(` and every rail SDK's client constructor, 0 hits across all 23 files that name a host.",
    "graphql.testnet.sui.io": "Sui's advertised GraphQL endpoint, asserted as the constant the manifest publishes.",
    "hashscan.io": "An endpoint this rail's manifest ADVERTISES, asserted as a value by the manifest's own constants test. That file contains no call site — swept for `fetch`, `axios`, `.request(` and every rail SDK's client constructor, 0 hits across all 23 files that name a host.",
    "horizon-testnet.stellar.org": "An endpoint this rail's manifest ADVERTISES, asserted as a value by the manifest's own constants test. That file contains no call site — swept for `fetch`, `axios`, `.request(` and every rail SDK's client constructor, 0 hits across all 23 files that name a host.",
    "horizon.stellar.org": "An endpoint this rail's manifest ADVERTISES, asserted as a value by the manifest's own constants test. That file contains no call site — swept for `fetch`, `axios`, `.request(` and every rail SDK's client constructor, 0 hits across all 23 files that name a host.",
    "integraledger.com": "An issuer/authority identifier inside a discovery fixture — a name in a document, compared as a string.",
    "json-schema.org": "A `$schema` identifier. Asserted as the value a document declares; no validator here fetches a meta-schema.",
    "legalcontextprotocol.org": "LCP's own published schema identifier, asserted as the `$schema` string a document must carry.",
    "mainnet-public.mirrornode.hedera.com": "An endpoint this rail's manifest ADVERTISES, asserted as a value by the manifest's own constants test. That file contains no call site — swept for `fetch`, `axios`, `.request(` and every rail SDK's client constructor, 0 hits across all 23 files that name a host.",
    "mainnet.sorobanrpc.com": "An endpoint this rail's manifest ADVERTISES, asserted as a value by the manifest's own constants test. That file contains no call site — swept for `fetch`, `axios`, `.request(` and every rail SDK's client constructor, 0 hits across all 23 files that name a host.",
    "one.one.one.one": "⭐ THE 'BY NAME' HALF OF THIS GATE'S OWN IPv6 PLANT, and nothing else in the tree. `rail-invariants/test/hermetic-tests-invariant.test.ts` writes this host and the address literal `[2606:4700:4700::1111]` — the SAME public resolver, twice — into temporary fixture trees, and asserts the gate refuses both. Before the address-literal fix it refused only the name: `[` was outside the host pattern's character class, so an address literal matched nothing at all and a test reaching a third party over one passed with no declaration needed. It is never resolved: the drive's fixtures are `mkdtemp` directories the gate READS and nothing executes. ⚠️ It needs an entry because this repository's drives live under `packages/`, which the gate walks. ⛔ Closed in both directions — if the drive stops planting it, this entry goes stale and the gate refuses it, so the exemption cannot outlive the plant.",
    "preprod.cardanoscan.io": "An endpoint this rail's manifest ADVERTISES, asserted as a value by the manifest's own constants test. That file contains no call site — swept for `fetch`, `axios`, `.request(` and every rail SDK's client constructor, 0 hits across all 23 files that name a host.",
    "readthedocs.org": "A documentation URL inside a fixture document, compared as a string.",
    "registry.npmjs.org": "`check-published-parity.mjs`'s `REGISTRY_ORIGIN` default. ⭐ The registry is an injectable SEAM and `published-parity-invariant.test.ts` drives every verdict against a LOCAL `node:http` server it starts itself — the default origin is never the one the non-live test uses. This is the shape the standing handoff names: a control that fires only because the network call was made a seam. ⛔⛔ AND IT WAS FALSE FROM 2026-09-15T18:09:46Z TO 2026-09-16T05:03:01Z — eleven hours, not eleven days, and the first cut of this sentence said days. The interval is written as its two endpoints because a unit is the part of a duration that rots silently: `git log -1 --format=%cI` on the commit that landed the live control and on the one that removed it. `published-dist-parity-invariant.test.ts` is the OTHER non-live test that reaches this module, and from the first of those timestamps it carried a live control built on `NetworkRegistry({})` — the default origin, genuinely fetched, 31 metadata reads and 31 tarballs on every `pnpm verify`. This gate declares by HOST over the whole tree and not by file, so the entry written for the sibling drive vouched for it by coincidence and stage 7 stayed green over a claim that had stopped being true. That control moved to `.github/workflows/published-dist-parity.yml` on 2026-09-16 (tracked on the planning register as row 281) and the claim holds again for BOTH importers. ⚠️ A declaration naming one file cannot notice a second one arriving: if a third non-live test imports this module, read what it does with the origin before trusting this entry. ⭐ That this table keys on the HOST and not on the FILE is the shape that let the claim go stale, and it is tracked on the planning register as row 290 rather than worked around here.",
    "rpc.moderato.tempo.xyz": "An endpoint this rail's manifest ADVERTISES, asserted as a value by the manifest's own constants test. That file contains no call site — swept for `fetch`, `axios`, `.request(` and every rail SDK's client constructor, 0 hits across all 23 files that name a host.",
    "rpc.tempo.xyz": "Recorded in a FIXTURE as the endpoint a mainnet transfer was observed at — provenance for captured bytes, not a target.",
    "s.altnet.rippletest.net": "An endpoint this rail's manifest ADVERTISES, asserted as a value by the manifest's own constants test. That file contains no call site — swept for `fetch`, `axios`, `.request(` and every rail SDK's client constructor, 0 hits across all 23 files that name a host.",
    "s1.ripple.com": "XRPL's advertised public cluster endpoint, asserted as a constant in the adapter's test.",
    "scan.global.dev.sync.global": "An endpoint this rail's manifest ADVERTISES, asserted as a value by the manifest's own constants test. That file contains no call site — swept for `fetch`, `axios`, `.request(` and every rail SDK's client constructor, 0 hits across all 23 files that name a host.",
    "scan.sync.global": "An endpoint this rail's manifest ADVERTISES, asserted as a value by the manifest's own constants test. That file contains no call site — swept for `fetch`, `axios`, `.request(` and every rail SDK's client constructor, 0 hits across all 23 files that name a host.",
    "soroban-testnet.stellar.org": "An endpoint this rail's manifest ADVERTISES, asserted as a value by the manifest's own constants test. That file contains no call site — swept for `fetch`, `axios`, `.request(` and every rail SDK's client constructor, 0 hits across all 23 files that name a host.",
    "testnet.mirrornode.hedera.com": "An endpoint this rail's manifest ADVERTISES, asserted as a value by the manifest's own constants test. That file contains no call site — swept for `fetch`, `axios`, `.request(` and every rail SDK's client constructor, 0 hits across all 23 files that name a host.",
    "testnet.xrpl.org": "An endpoint this rail's manifest ADVERTISES, asserted as a value by the manifest's own constants test. That file contains no call site — swept for `fetch`, `axios`, `.request(` and every rail SDK's client constructor, 0 hits across all 23 files that name a host.",
    "ucp.dev": "A UCP namespace identifier carried in fixture documents, compared as a string.",
    "www.w3.org": "The W3C Verifiable Credentials `@context` URI. A JSON-LD context is an IDENTIFIER in a credential document — the authority tests assert the string a grant carries, and nothing dereferences it.",
    "x": "A placeholder tarball URL — `http://x/t.tgz` — inside the registry METADATA the parity drive injects into its own local server. It is never resolved: the drive answers every request itself, and `x` exists so the fixture has a syntactically valid URL to carry.",
    "xrpl.org": "An endpoint this rail's manifest ADVERTISES, asserted as a value by the manifest's own constants test. That file contains no call site — swept for `fetch`, `axios`, `.request(` and every rail SDK's client constructor, 0 hits across all 23 files that name a host.",
    "xrplcluster.com": "An endpoint this rail's manifest ADVERTISES, asserted as a value by the manifest's own constants test. That file contains no call site — swept for `fetch`, `axios`, `.request(` and every rail SDK's client constructor, 0 hits across all 23 files that name a host."
  },
  "$comment2": "Every THIRD-PARTY import a non-live test makes, classified. A host scan cannot see a client that takes its address from process.env, so every import is accounted for as one of three kinds. inert: it reaches no network at all. addressed-in-source: it can, and any endpoint it is pointed at is written in the file, where the host scan sees it — loopback included, which this gate declares hermetic. endpoint-from-environment: it can, and its address does NOT appear in the file, so a test importing one MUST gate itself on the variable it needs or be a live rail harness. CLOSED IN BOTH DIRECTIONS. Classifications for packages this repository shares with the private seller-side repository are carried across with their reasons rather than re-derived; the four multiformats/bs58 entries are this repository's own.",
  "imports": {
    "@aptos-labs/ts-sdk": {
      "kind": "addressed-in-source",
      "why": "a chain client whose node URL is written into the fixture that builds it"
    },
    "@cfworker/json-schema": {
      "kind": "inert",
      "why": "a JSON-Schema validator; it dereferences nothing"
    },
    "@mysten/sui/transactions": {
      "kind": "inert",
      "why": "transaction construction; the client that would submit one is a separate subpath this suite does not import"
    },
    "@stellar/stellar-sdk": {
      "kind": "addressed-in-source",
      "why": "same"
    },
    "bs58": {
      "kind": "inert",
      "why": "Base58 encode/decode and nothing else. binding-solana's `src/adapter.ts` imports the same module to read a key pair out of its textual form; there is no transport in it."
    },
    "fast-check": {
      "kind": "inert",
      "why": "property generation, in-process"
    },
    "multiformats/cid": {
      "kind": "inert",
      "why": "CID construction. `evidence/src/car.ts` imports the same module to build content identifiers for a CAR file; multiformats is deliberately transport-free — fetching a CID is a different package."
    },
    "multiformats/codecs/raw": {
      "kind": "inert",
      "why": "The raw multicodec, a two-function encoder. Same import as `evidence/src/car.ts`."
    },
    "multiformats/hashes/digest": {
      "kind": "inert",
      "why": "Multihash digest construction from bytes already in hand. Same import as `evidence/src/car.ts`."
    },
    "node:child_process": {
      "kind": "addressed-in-source",
      "why": "⚠️ it can spawn anything, including something that reaches a network. What bounds it is the command, which is written in the file — this gate reads the imports, and a reviewer reads the command"
    },
    "node:crypto": {
      "kind": "inert",
      "why": "hashing and randomness"
    },
    "node:fs": {
      "kind": "inert",
      "why": "the local filesystem"
    },
    "node:http": {
      "kind": "addressed-in-source",
      "why": "used to BIND a loopback server, which this gate declares hermetic in its own head note — and any address it is pointed at is a string the host scan reads"
    },
    "node:module": {
      "kind": "inert",
      "why": "`createRequire` for resolving a path. No transport."
    },
    "node:os": {
      "kind": "inert",
      "why": "temp directories and platform facts"
    },
    "node:path": {
      "kind": "inert",
      "why": "string manipulation"
    },
    "node:process": {
      "kind": "inert",
      "why": "argv, env and exit. No transport."
    },
    "node:url": {
      "kind": "inert",
      "why": "URL parsing — parsing an address is not dialling one"
    },
    "node:zlib": {
      "kind": "inert",
      "why": "gunzip of a tarball already in hand. No transport."
    },
    "viem": {
      "kind": "addressed-in-source",
      "why": "the EVM client. A transport is constructed from a URL the file states, or from an injected port"
    },
    "viem/accounts": {
      "kind": "inert",
      "why": "key derivation and signing, no transport"
    },
    "vitest": {
      "kind": "inert",
      "why": "the test framework"
    }
  }
}
